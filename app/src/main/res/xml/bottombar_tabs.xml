<?xml version="1.0" encoding="utf-8"?>

<tabs>

    <tab
        id="@+id/tab_home"
        icon="@drawable/ic_home"
        title="Home"
        activeColor="@color/Blue700"
        barColorWhenSelected="@color/White"/>

    <tab
        id="@+id/tab_favorites"
        icon="@drawable/ic_favorite_black_24dp"
        title="Favorites"
        activeColor="@color/Blue700"
        barColorWhenSelected="@color/White"/>

    <tab
        id="@+id/tab_history"
        icon="@drawable/ic_history"
        title="History"
        activeColor="@color/Blue700"
        barColorWhenSelected="@color/White"/>

    <tab
        id="@+id/tab_recordings"
        icon="@drawable/ic_voicemail"
        title="Recordings"
        activeColor="@color/Blue700"
        barColorWhenSelected="@color/White"/>

</tabs>